import { keyStatus } from "./PuyoTypes";

type IProps = {
  keys: keyStatus;
  setKeys: React.Dispatch<React.SetStateAction<keyStatus>>;
};
export const PuyoKeys = (props: IProps) => {
  return <></>;
};
